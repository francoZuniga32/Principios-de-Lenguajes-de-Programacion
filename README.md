# Principios-de-Lenguajes-de-Programacion

## Instalacion del script compilador

```bash
cp ~/.local/bin/ghcompile.sh
``` 

puede ejecutarlos usando `ghcompile archivo`. es importante aclarar que no tiene que llevar el .hs al final